{
  "name": "az400-canary-release",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "npm test && npm audit --audit-level=high"
  },
  "dependencies": {
    "@azure/app-configuration": "^1.9.0",
    "@azure/identity": "^4.4.1",
    "applicationinsights": "^3.7.0",
    "express": "^4.19.2"
  }
}
